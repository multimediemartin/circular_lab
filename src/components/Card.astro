---
const Date = Astro.props.Date;
const Header = Astro.props.Header;
const Description = Astro.props.Description;
const Link = Astro.props.Link;
const { product } = Astro.props;
---

<div>
  <a href=`${product.id}`>
    <h2>{product.event_title}</h2>
    <h3>{product.event_date}</h3>
    <p>
      {product.event_short_description}
    </p>
  </a>
</div>
<style>
  h2 {
    text-align: left;
    font-family: "Helvetica-Neue-LT-Std";
    font-size: 32px;
    font-style: normal;
    font-weight: 900;
    line-height: normal;
  }
  h3 {
    font-family: "Helvetica-Neue-LT-Std";
    font-size: 32px;
    font-style: normal;
    font-weight: 900;
    line-height: normal;
    font-size: 32px;
    place-self: center;
    padding: 10px;
    background-color: var(--red);
    color: white;
    text-transform: uppercase;
  }
  div {
    background-color: var(--white);
    margin: 20px;
    padding: 20px;
    min-height: 400px;
    border-radius: 20px;
    transition: var(--transition);
  }

  a {
    text-decoration: none;
    color: var(--black);
  }
  div:hover {
    background-color: var(--dark);
    transition: var(--transition);
  }
  div:hover a {
    color: var(--white);
  }
</style>
